spring.datasource.url=jdbc:mysql://localhost:3306/petcare?useSSL=false&serverTimezone=Europe/Madrid&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=toor

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Enable schema.sql execution
spring.sql.init.mode=always

spring.thymeleaf.cache=false

# --- Credenciales y Scopes ---
spring.security.oauth2.client.registration.google.client-id=287224420958-mn5u1l0v7kbhn5n8o6vb8n48t5037hhc.apps.googleusercontent.com
spring.security.oauth2.client.registration.google.client-secret=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
spring.security.oauth2.client.registration.google.scope=openid,profile,email

# --- URI de Redirección (¡Punto del error!) ---
# Spring Security espera el patrón por defecto: /login/oauth2/code/{registrationId}
spring.security.oauth2.client.registration.google.redirect-uri=http://localhost:8080/login/oauth2/code/google

# --- Endpoints del Proveedor (Opcional, pero bueno tenerlos) ---
# Google es un proveedor común, por lo que estas URIs suelen ser las que Spring utiliza por defecto.
spring.security.oauth2.client.provider.google.authorization-uri=https://accounts.google.com/o/oauth2/auth
spring.security.oauth2.client.provider.google.token-uri=https://oauth2.googleapis.com/token
spring.security.oauth2.client.provider.google.user-info-uri=https://www.googleapis.com/oauth2/v2/userinfo
spring.security.oauth2.client.provider.google.user-name-attribute=id
spring.security.oauth2.client.provider.google.jwk-set-uri=https://www.googleapis.com/oauth2/v3/certs
spring.security.oauth2.client.provider.google.issuer-uri=https://accounts.google.com


# Config SMTP (si usáis servidor de correo tipo SendGrid SMTP o similar)
spring.mail.host=smtp.tu-servidor.com
spring.mail.port=587
spring.mail.username=TU_EMAIL
spring.mail.password=TU_PASSWORD
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Email configuration
# app.mail.from=your-email@example.com
# app.frontend.base-url=http://localhost:8080
